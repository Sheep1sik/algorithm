#include <bits/stdc++.h>
using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(NULL);

    // 더 간단한 코드 O(1)
    long long n;
    cin >> n;

    cout << n * (n + 1) / 2 << "\n";

    // int n,t;
    // t = 0;
    // cin >> n;
    
    // for (int i = 1; i <= n; i++) {
    //     t = t + i;
    // }

    // cout << t << "\n";

    return 0;
}
