#include <bits/stdc++.h>
using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(NULL);

    long long a,b,c;
    cin >> a >> b >> c;
    cout << (long long)a + b + c << "\n";

    return 0;
}
